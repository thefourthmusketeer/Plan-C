# ─── Cloudflare ───────────────────────────────────────────────────────────────
# API token — needs Zone:DNS:Edit permission for your zone
CF_TOKEN=

# Zone ID — find it in Cloudflare dashboard → your domain → right sidebar on the bottom (scroll to the end)
CF_ZONE_ID=

# ─── Domain ───────────────────────────────────────────────────────────────────
# Root domain (must be the zone managed by Cloudflare above)
ROOT_DOMAIN=yourdomain.com

# Subdomain prefix for the CDN alias (results in cdn.yourdomain.com)
# This is what clients put in SNI — must not be proxied in Cloudflare
CDN_SUBDOMAIN=cdn

# ─── xray / 3x-ui inbound ────────────────────────────────────────────────────
# Port xray listens on (CloudFront → VPS)
INBOUND_PORT=443

# WebSocket path — use something plausible-looking
WS_PATH=/videos/watch

# VLESS UUID — leave blank to auto-generate
VLESS_UUID=

# ─── 3x-ui panel (optional — defaults: admin/admin) ─────────────────────────
# If you changed the panel credentials from defaults, set them here
# XUI_USERNAME=admin
# XUI_PASSWORD=admin
# XUI_PORT=2053
